import SensorClient from "./MySensorClient";

export const metadata = {
  title: "Sensors || TheBotsWorld",
  description: "TheBotsWorld - Manage your Robot's sensors",
};

export default function SensorPage() {
  return <SensorClient />;
}
